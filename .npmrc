# Flat node_modules layout — maximizes compatibility with the AWS SAM esbuild
# bundler (which doesn't traverse pnpm's symlinked store cleanly).
node-linker=hoisted
auto-install-peers=true
strict-peer-dependencies=false
